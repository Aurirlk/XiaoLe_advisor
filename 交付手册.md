# 小乐AI · 高考志愿填报助手 — 用户交付手册

> **版本**：v0.3.0（2026-07-31 架构升级后）
> **本文档替代原《交接手册》**：原 51 项问题（14 P0 / 22 P1 / 15 P2）已按优先级完成修复与处置，本文档面向接手开发/运维人员，说明"这个系统是什么、怎么跑起来、怎么改、有哪些已知边界"。
> **v0.3.0 新增**：真流式输出、Embedding 模型统一、RAG 评估体系、RAG 融合权重配置化、意图离线沉淀（intent_tracker 写入链路接通）。

---

## 一、项目概览

**小乐AI** 是一个面向高考志愿填报场景的多智能体对话系统：考生/家长通过文字、语音或图片提问，系统经 **LangGraph 多智能体编排**完成画像收集 → 场景识别 → 数据查证（SQL/Neo4j/联网）→ 风险风控 → 张雪峰风格合成建议，并支持 TTS 语音回复、ASR 语音输入、CRM 画像断点续传。

**核心特征**：
- 8+ 专业 agent 分工（supervisor / profile / match / career / web_search / sql / synthesis / family / parent）
- 确定性风控层（synthesis_guard / red_team_auditor / risk_assessor / reality_checker）——**回复不越界、不编数据**
- 全参数化 SQL/Cypher（无注入风险），SSRF 防护
- RAG 经验库 + Neo4j 知识图谱 + 联网搜索三路融合
- 语音全链路：VAD → ASR（MiMo/OpenAI 兼容）→ LLM → TTS（edge/MiniMax）流式输出

---

## 二、技术栈与架构

| 层 | 技术 |
|---|---|
| 前端 | Vue3 + Vite + Element Plus（`frontend/`，SPA，已接 DOMPurify XSS 防护） |
| 后端 | FastAPI + Uvicorn（`api/`） |
| 编排 | LangGraph 1.x（`core/graph_builder.py`，Checkpoint 持久化多轮状态） |
| 数据 | SQLite（默认）/ PostgreSQL（可选）、Neo4j 图库、Chroma 向量库 |
| 异步任务 | Celery + Redis（broker db1 / result db2） |
| 语音 | edge-tts / MiniMax TTS / MiMo ASR / Silero VAD |

**多智能体图结构**（`core/graph_builder.py`）：

```
用户输入 → supervisor_agent（场景/路径/决心度路由）
   ├→ profile_agent      画像收集（省份/选科/意向专业=核心三要素）
   ├→ parent_agent       家长画像
   ├→ family_agent       家庭融合
   ├→ match_agent        分数线/位次查证（Neo4j 优先 + SQLite 降级）
   ├→ career_agent       就业前景（RAG+Neo4j+投研数据+AI暴露度 四源并行）
   ├→ web_search_agent   联网搜索（关键词提取 + 平台分类）
   ├→ sql_agent          结构化 SQL 问答
   ├→ decision_detector  犹豫检测（只产结构化 hints，不重复生成回复）
   └→ synthesis_agent    最终合成（风控 + 张雪峰风格）
```

**关键设计决策（修复后）**：
- 画像完整性判据 = `province / subject_type / major_name` 核心三要素（`core/state_schema.py` 单一真源，supervisor 与 profile_agent 共用，杜绝死循环）
- 代码缺陷异常（NameError/KeyError 等）不再伪装成"服务繁忙"，开发环境直接暴露（`core/exception_handler.py`）
- 手机号 SHA-256 脱敏存储（`PHONE_HASH_SALT`），不落明文

---

## 三、快速部署

### 1. 环境准备

```bash
# 推荐 conda 环境（本机已有 zxf，依赖齐全）
conda activate zxf
pip install -r requirements.txt        # 已锁定版本区间
pip install pytest-forked              # 测试用（可选）
```

> ⚠️ zxf 环境已知问题：`import core.emotion_analyzer` 后再 `import api.dependencies` 偶发 Segfault（sentence_transformers 原生库顺序冲突）。与代码无关，正常启动路径不受影响；单文件/小组测试稳定。

### 2. 配置

```bash
cp .env.example .env
# 必填（生产）：JWT_SECRET、PHONE_HASH_SALT、NEO4J_PASSWORD、MIMO_API_KEY、ADMIN_API_KEY
# 可选：ZHIHU_ACCESS_SECRET（知乎搜索代理）、CHECKPOINT_BACKEND=postgres + DATABASE_URL
```

`configs/.config.yaml` 中的 API Key 一律使用 `${ENV_VAR}` 占位符（已移除明文，且文件已从 git 跟踪移除）。

### 3. 初始化与启动

```bash
python scripts/init_sqlite.py          # 建表 + 种子数据 + 自动迁移（旧表补列/手机号哈希）
python -m api.main                     # 或 uvicorn api.main:app --reload
celery -A celery_app worker -Q default,rag,crm --loglevel=info   # 可选
celery -A celery_app beat --loglevel=info                        # 可选
cd frontend && npm install && npm run dev   # 前端开发模式（默认 5173）
```

> 首次启动 lifespan 会：初始化 SQLite → 检查 Redis → 编译 LangGraph（含 Checkpoint 异步初始化）→ 可选启动 Checkpoint TTL 每日清理（`CHECKPOINT_TTL_DAYS>0`）。

### 4. CI

```bash
# .github/workflows/ci.yml 已就绪：Ubuntu + Python 3.11 + 全量测试（默认排除 llm 标记）
pytest                     # 本地跑（默认 -m "not llm"）
pytest -m llm              # 显式跑 LLM 评测（需配 DEEPSEEK_API_KEY，会烧钱）
```

---

## 四、环境变量全表

| 变量 | 必填 | 说明 |
|---|---|---|
| `JWT_SECRET` | 生产必填 | ≥32 字符；未配置时生产启动失败（fail-fast） |
| `PHONE_HASH_SALT` | 生产必填 | 手机号脱敏盐；开发缺省用默认盐（仅本地） |
| `MIMO_API_KEY` / `DEEPSEEK_API_KEY` 等 | 按引擎 | LLM/ASR/TTS 各供应商 Key |
| `NEO4J_URI/USER/PASSWORD` | 用图谱时 | 已去默认弱密码 |
| `ADMIN_API_KEY` | 生产建议 | 保护 `/api/admin*` 写接口（无则 503） |
| `ZHIHU_ACCESS_SECRET` | 用知乎搜索时 | `/api/zhihu/search` 代理密钥（原硬编码已移除） |
| `CHECKPOINT_BACKEND` | 生产建议 | `memory`（仅开发）/`sqlite`/`postgres`（生产推荐，禁 memory） |
| `DATABASE_URL` | postgres 时 | Checkpoint 后端连接串 |
| `CHECKPOINT_TTL_DAYS` | 可选 | >0 时每日清理超期 Checkpoint |
| `CORS_ALLOW_ORIGINS` | 生产必配 | 禁止 `*`（生产强制） |
| `API_AUTH_REQUIRED` | 可选 | 生产恒强制鉴权；开发置 1 打开 |
| `WS_MAX_CONN` / `WS_IDLE_TIMEOUT_SECONDS` | 可选 | WS 并发上限/空闲超时（默认 200/120s） |
| `MAX_*_UPLOAD_MB` / `RATE_LIMIT_*` | 可选 | 上传上限与高成本端点限流 |

---

## 五、功能与 API 地图

| 模块 | 端点 | 说明 |
|---|---|---|
| 对话 | `POST /chat/*`、`GET/POST /stream/*`、`WS /ws/chat` | 主对话链路（HTTP 流式 + WS 全双工） |
| 认证 | `POST /auth/register|login`、`GET /auth/me` | JWT；禁自注册 admin；手机号哈希存储 |
| 多模态 | `POST /voice/asr|tts`、`WS /voice/tts-stream`、`POST /vision/analyze|chat` | 语音识别/合成/图片理解（已加鉴权+限流+大小限制） |
| RAG | `POST /rag/upload`、`/rag/rebuild` 等 | 文档入库（chunk 切分 512/64）、索引重建 |
| Harness | `POST /harness/upload*`、`GET /harness/scenarios` | 数据文件上传解析入库 |
| 管理 | `/admin/*`、`/api/admin/*` | 运营 CRUD（X-Admin-Key 保护） |
| 知乎搜索 | `GET /api/zhihu/search` | 知乎 MCP 代理（密钥走环境变量） |
| 学习路径 | `GET /api/career/paths` 等 | 升学路径模板 + 文档检索 |
| 健康检查 | `GET /healthz` | 存活探针 |

**语音格式契约**：ASR 接受 `audio/webm`、`audio/wav`、`audio/mp3` 等（白名单）；TTS 返回 `audio/mpeg`。前端注意 48kHz 立体声 WebM 与后端 16k/mono 的转换（见"已知边界"）。

---

## 六、测试与质量

- **单元测试**：`tests/`（23 个文件，200+ 用例，含路由黄金用例 30 条、风控/工具层/状态机）
- **CI**：`.github/workflows/ci.yml` —— push/PR 触发，含"单一 graph_builder.py 真源"防重复代码树回归
- **默认排除 LLM 用例**（`pytest.ini` `-m "not llm"`），防 CI 烧钱
- **本机验证**：核心链路（admin 路由 / supervisor 路由 / 异常兜底 / Checkpoint / 工具重试）98 passed

---

## 七、安全与合规（修复成果）

| 项 | 状态 |
|---|---|
| 密钥入库 | ✅ 已移除明文（`${ENV_VAR}`），`configs/.config.yaml` 已取消 git 跟踪并加入 .gitignore |
| 认证绕过 | ✅ 登录真校验密码、禁自注册 admin、`/me` 强制鉴权 |
| JWT 弱密钥 | ✅ 生产 fail-fast + 长度告警 |
| 前端 XSS | ✅ DOMPurify + marked 白名单渲染（4 处 v-html 全接入） |
| 上传接口 | ✅ 流式限长(413) + MIME 白名单 + Pillow 图像头校验 + 鉴权 + 限流 |
| WebSocket | ✅ token 鉴权(4401) + 连接配额(1013) + 空闲超时 |
| 手机号 PII | ✅ SHA-256 + 盐脱敏存储，存量自动迁移 |
| CORS | ✅ 生产禁止 `*` |
| 管理员接口 | ✅ `/api/admin*` 强制 X-Admin-Key |

---

## 八、已知边界与后续建议（接手人必读）

以下为**架构级/产品级**事项，修复工程未纳入（工程量超 bug 修复范畴），建议按序推进：

1. ~~**真流式输出**（原 P1-1）~~ ✅ **v0.3.0 已解决**：`/stream/advice` 与 `WS /ws/chat` 改用 `stream_mode=["updates","messages"]` 双流并行；`synthesis_agent` 改 `llm.astream` 聚合（原 `ainvoke` 走一次性生成，token 回调不触发）；`dependencies.py` 的 LLM 构造开启 `streaming=True`；路由层只放行 `AIMessageChunk` 增量（过滤 supervisor 推理与最终完整 AIMessage，防重复拼接）；无流式时自动兜底整段。
2. **Human-in-the-loop**（原 P1-4）：LangGraph `interrupt`/`Command` 支持人工确认（如"冲/稳/保"二次确认）。
3. ~~**Embedding 模型统一**（原 P1-9）~~ ✅ **v0.3.0 已解决**：模型名统一从 `vector_config.yaml` 单一真源解析（`tools/embedding_config.py`），`vector_store` 与 `multi_embedding_store` 共用同一模型；本地模型目录优先（离线可用）；切换模型用 `python scripts/rebuild_embedding_index.py` 重灌。
4. ~~**RAG 评估体系**（原 P1-12）~~ ✅ **v0.3.0 已解决**：`scripts/rag_eval/`（30 条领域标注集 + Recall@k/Precision@k/MRR/NDCG@k 评估脚本，可选 LLM faithfulness）。
5. ~~**反幻觉升级**（原 P1-11）~~ 部分保留：当前为关键词规则级；claim-level NLI 校验 + chunk 级 citation 仍待做。
6. **Neo4j 客户端收敛**（原 P1-13）：`neo4j_tools`/`graph_rag`/`kg_client` 三处 driver，建议收敛为单例。
7. **VAD/TTS 引擎**（原 P1-14/15）：VAD 实为能量阈值器（Silero 模型缺失）；TTS 首包延迟需缓存预热。
8. **前后端音频契约**（原 P1-18）：前端 48k 立体声 WebM 未按 `asr_config.yaml` 的 16k/mono/AEC 采集；云端 ASR 依赖 ffmpeg 转换。
9. **前端工程**（原 P2-14）：axios 与 fetch 双 API 层并存、无 TS/Pinia、dist 18MB 未清理 —— 建议统一 apiClient + 引入 TS。
10. **黄金用例扩充**（原 P2-15）：`routing_golden.json` 30 条 → 目标 ≥100 条分层。

**v0.3.0 修复的隐藏缺陷（原 P1-P7 范围）**：
- **FTS5 全文检索路实际是死的**：`rag_fts` 表建了但从未灌数据（0 行）+ unicode61 tokenizer 对中文近乎无效。已改为 trigram tokenizer + 启动幂等灌入 2287 条语料（`scripts/init_sqlite.py`）。
- **Chroma 向量库只同步了 10/2287 条**：原 `collection_has_data()` "非空即跳过"导致永不同步补齐。已改为按条数比对，不足则重灌（`api/main.py`）。
- **中文分词失效**：`_tokenize` 原用 `re.split(r"\W+")` 对中文把整句当单 token，dense/sparse 打分失真。已改为 CJK 字符级 + 英文单词级混合分词。
- **intent_tracker 写入链路断裂**（真问题）：`log_intent`/`log_decision_journey` 此前零调用方。已在 CRM 回调接线（`api/dependencies.py` `_make_crm_callback`），并把 synthesis 结果合并进 state 再回调（此前回调读不到最终回复）；表 DDL 已接入 `init_sqlite.py`。
- **chromadb 1.5.9 + Windows 环境缺陷**（两个坑，均已规避）：
  1. `persist_dir` 用绝对路径（`D:\...`）打开 hnsw 段报 "Error loading hnsw index"，项目根相对路径正常 → `tools/vector_store.py` 统一 `_normalize_persist_dir` 转相对路径
  2. 非默认 `Settings(anonymized_telemetry=False)` 触发 SharedSystemClient identifier 冲突（同路径不同 settings）→ 改用默认 settings
  - 另有 `hnsw:sync_threshold=100` collection 元数据（默认 1000，2287 条语料尾部 239 条永不落盘）
- **RAG 融合权重标定**（评估驱动）：默认 `dense_weight` 1.0→0.3（30 条评估集标定：MiniLM 中文语义噪声大，满权重把 FTS5 优质结果挤出 top-k，Recall@3 0.556→0.356；调低后稳定 0.567）。更换更强 embedding 后用 `scripts/rag_eval` 重新标定。

**v0.4.0 架构升级（2026-08-06，多 Agent 知识库分权）**：
- **RAG 知识库分权（逻辑分库）**：`RAGTools` 新增 `kb_scope` 参数（`None`=全库 / `"majors"` / `["employment","majors"]` / 前缀列表），三路检索（Chroma `where` / FTS5 `source LIKE` / 关键词过滤）统一按知识域限定；`KB_SCOPES` 常量映射语料 source 前缀（majors/universities/employment/guides/policies）。**`career_agent` 已接入** `kb_scope=["user:employment/","user:majors/","user:policies/","user:guides/"]`（不查院校库）。**跨库兜底**：scope 内无结果时自动 fallback 全库并打 `fallback_cross_kb: True` 标记（供评估统计），域内有结果不跨库（防污染）。测试 `tests/test_rag_kb_scope.py`（14 用例）；评估指标无退化（Recall@3=0.567 / MRR=0.666 与分库前一致）。
- **多智能体协作体系重建（蓝图 Phase 3 落地，2026-08-06）**：
  - `core/agent_bus.py`（**重建**）：`AgentCommunicationBus`——消息订阅/发布、请求-响应、广播三模式；asyncio 友好、异常隔离、超时降级。测试 `tests/test_agent_bus.py`（8 用例）。
  - `core/reflexion_agent.py`（**重建**）：`ReflexionAgent`——输出质量评估（确定性规则，零 LLM 可离线）→ 改进建议 → 重试循环（上限控制）→ 反思记忆沉淀/复用（按主题 LRU）。测试 `tests/test_reflexion_agent.py`（7 用例）。
  - `core/result_fusion.py`（**激活**）：`build_graph(enable_result_fusion=True)` 时新增 `result_fusion` 节点（synthesis 前），收集 sql/career/web 多源输出用 ResultFusion 融合写入 `fusion_context`，synthesis 读取；开关关闭时保持原路径（兼容）。`AllowedNode`/`RouteDecision` 已补 `result_fusion`。
  - ⚠️ 说明：Agent Bus / Reflexion 已重建为可独立使用的骨架，**尚未接入具体 worker 的调用点**（bus 未在 graph_builder 实例化、reflexion 未挂到 match/career）——下一步按蓝图 3.1/3.2 的"接入 graph_builder + 修改 Worker"推进。
- **死代码标注**（不删，留待架构决策）：`tools/reranker.py`（CrossEncoder 重排）、`tools/graph_rag.py`（图谱多跳检索）、`_search_from_milvus/_search_from_es`（Milvus/ES 读路径）——零调用方，已加 `⚠️ 状态标注`。
- **面试叙述对齐**（架构蓝图）：write_Agent 已落地（见下）；match_agent 走 GraphRAG / decision_detector 决心值机制（试卷+知乎舆论+5轮）仍为【未实现】，记录为后续落地方向（见第八节 roadmap）。
- **write_Agent（蓝图落地，2026-08-06）**：`agents/workers/write_agent.py`——**唯一写权限** worker：接收 web 搜索结果 → 写前校验（噪声过滤 MIN_TEXT_LEN）/去重（source hash，块号归一化）→ 同步双写（RAG JSON `user:web/` 域 + Chroma）。接入 `build_graph(enable_write_agent=True)`：`web_search_agent → write_agent → (fusion) → synthesis`。测试 `tests/test_write_agent_xuefeng.py`。
- **张雪峰对话语料库（蓝图落地，2026-08-06）**：`tools/xuefeng_store.py` + `scripts/build_xuefeng_corpus.py`——对话切块（轮次边界，超长按句）、学科 embedding（复用统一模型，向量空间与主 RAG 一致）、**Milvus 后端（`MILVUS_ENABLED=1` 时启用 HNSW/COSINE，服务不可达自动降级 Chroma `zx_xuefeng` collection）**、专业关键字窗口词检索（2-3 字窗口 + 向量混合，向量失败关键字兜底）。`KB_SCOPES` 加 `xuefeng` 域；**synthesis_agent 接入**（`xuefeng_store` 参数，生成时检索雪峰原话注入 `xuefeng_style_refs` 作为风格锚点）。内置 3 条种子语料（医学/冲稳保/城市vs学校）可验证链路；真实对话语料放 `data/xuefeng_dialogues/` 后跑脚本入库。
- **项目铁律**（用户明确要求）：删除任何文件前必须对照蓝图文档（`.omo/plans/xiaole-ai-master-plan.md` / `docs/技术文档.md`）确认非蓝图组件，并向用户确认。

---

## 九、代码结构地图

```
api/                     FastAPI 应用
  ├── main.py            入口 + lifespan（模块加载/DB/Redis/Graph/TTL）
  ├── dependencies.py    依赖注入（LLM/RAG/Graph/CRM/Redis 单例）
  ├── security_utils.py  上传限制/限流/WS 鉴权（P0-10/11 基建）
  ├── crud_services.py   运营 CRUD 服务层（自 backend 迁移）
  ├── response.py        统一响应封装
  └── routers/           23 个路由
agents/                  多智能体
  ├── supervisor_agent.py   场景/路径/决心度路由（黄金用例 30 条）
  ├── synthesis_agent.py    最终合成（张雪峰 prompt + 风控）
  ├── decision_detector.py  犹豫检测（结构化 hints）
  └── workers/            8 个执行 agent
core/                    核心
  ├── state_schema.py    状态定义 + 画像判据单一真源 + reducers
  ├── graph_builder.py   图编译（唯一真相源）
  ├── checkpoint_manager.py  Checkpoint（memory/sqlite/postgres + TTL）
  ├── crm_manager.py     用户画像（DDL 单一真相源 + 手机号哈希）
  ├── exception_handler.py  safe_node_call（缺陷异常暴露）
  └── providers/         ASR/TTS/VLLM 工厂（无静默降级）
skills/                  确定性技能（14 个活跃，6 个死代码已删）
tools/                   工具层（SQL/Neo4j/RAG/搜索/向量）
configs/
  ├── .config.yaml       运行时配置（Key 全走 ${ENV_VAR}）
  ├── prompts/           系统提示词（zx_system_prompt.md 为主）
  ├── asr_config.yaml / tts_config.yaml / llm_config.yaml
  └── routing_tuning.yaml  路由关键词调优（原子写 + 缓存）
frontend/                Vue3 SPA（src/utils/sanitize.js 为 XSS 安全渲染）
tests/                   pytest 套件（23 文件）
scripts/                 初始化/爬虫/导入脚本
.github/workflows/ci.yml CI
```

**Prompt 工作流说明**（调研结论）：
- 主系统提示词 `configs/prompts/zx_system_prompt.md`（张雪峰风格 + 硬性反幻觉规则）由 synthesis_agent 在每次合成时读取，`synthesis_system_prompt.txt` 为缺失时的兜底
- supervisor / decision_detector 等 agent 使用各自模块内嵌 prompt
- 修改话术：改 `zx_system_prompt.md` 即可全局生效；**不要**在 agent 代码里硬编码新风格

---

*本手册由修复工程产出，随代码更新。发现出入请直接修订本文档并标注日期。*
