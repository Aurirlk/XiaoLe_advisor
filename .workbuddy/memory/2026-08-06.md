# 2026-08-06

## Grill-Me 架构拷打 + 知识库分权落地（v0.4.0）

### 拷打背景
用户用 grill-me skill 拷打项目多 Agent + RAG 架构，并对照 E:/面试/思美软件-技术面试.md 的 Q4 多 Agent 回答。
**面试叙述 vs 代码真相对照结论**（重要，影响可信度）：
- 面试称 8 Agent 含 write_Agent（唯一写权限）→ **实际不存在**，实际是 sql_agent
- 面试称 match_agent 先 GraphRAG 查本地知识库 → **graph_rag.py 零调用**
- 面试称 decision_detector 有试卷/知乎舆论/5轮决心值 → **实际只产结构化 hints**
- 面试称 Supervisor 亲自用 MCP 回答 → 实际无 MCP 调用
- 真实项：career_agent 四源并行(RAG+Neo4j+投研+AI暴露度)、profile/parent/family 规则型 agent、Supervisor-Worker 路由
- 结论：方向对、细节虚——三处"理想架构"未实现，已作为 roadmap

### 分块决策（分支1）
保持长度/句法分块（build_rag_index 段落→句子 1200 字符；rag_router 512/64 递归）。
不上语义分块：语料无雪峰对话场景，语义切分无素材基础。未来加对话语料时按类型分流（结构化走长度分块、对话走窗口/语义分块）。

### 检索后端决策（分支2）
保持 local_file + Chroma。Milvus/ES 是只写不读死链路（backend=local_file 时 _search_from_milvus/es 永不执行），已标注未启用。规模 >5 万条再切。

### 落地：RAG 知识库分权（逻辑分库）
- tools/rag_tools.py：新增 KB_SCOPES 常量 + _normalize_scope/_in_scope；三路检索（_hybrid_recall/_search_fts5/_search_embedding/_local_search）全加 prefixes 过滤；入口 query_zx_experience(_top_docs/async) 加 kb_scope 参数；跨库兜底（scope 空→全库+fallback_cross_kb 标记）
- tools/vector_store.py：ChromaVectorStore.query 加 source_prefixes（where $or + $like）
- agents/workers/career_agent.py：接入 kb_scope=["user:employment/","user:majors/","user:policies/","user:guides/"]
- 测试 tests/test_rag_kb_scope.py（14 用例 passed）；评估无退化（Recall@3=0.567/MRR=0.666）

### 死代码标注（零调用方，不删）
reranker.py（CrossEncoder 重排）、core/result_fusion.py、tools/graph_rag.py、_search_from_milvus/es 均加 ⚠️ 状态标注注释。

### 遗留拷打分支（未完成，用户可继续）
分支3 重排（reranker 接入条件）、分支4 Embedding 模型（MiniLM→BGE-M3）、分支5 规则型 vs LLM agent 边界、分支6 死代码四选一。

## 2026-08-06 第二轮：蓝图对照 + 多智能体协作体系重建

### 重大认知（用户拷打后）
1. **项目定位 = 作品集**（面试展示），非上线产品。架构决策标准 = 能否讲清楚/可 review/与简历叙事自洽。
2. **产品叙事主线**：多 Agent 协同（中枢-员工 Sisyphus 模式）+ RAG/Neo4j/GraphRAG 知识库 + Harness/multi-agent 学习协作机制的高考志愿决策系统。
3. **"一家人吵架" = 用户群体画像**，不是架构模式（纠正了误读）。
4. **蓝图文档是真相源**：`.omo/plans/xiaole-ai-master-plan.md`（699行）、`docs/技术文档.md`（12大体系）、`docs/版本历史.md`。之前审查漏了这层！
5. **教训（用户明确批评）**：agent_bus/reflexion_agent/evaluation 曾因"零引用"被删（P2-1），实为蓝图"多智能体协作体系"核心 → 已立项目铁律：删除前必须对照蓝图+用户确认。

### 蓝图 Phase 3 重建完成
- **core/agent_bus.py 重建**：AgentCommunicationBus（订阅/发布 + 请求-响应 + 广播，asyncio 友好、异常隔离、超时降级）。8 用例过。
- **core/reflexion_agent.py 重建**：ReflexionAgent（规则评估 → 建议 → 重试循环 → 反思记忆沉淀/复用，零 LLM 可离线；topic 自动从 context.scene 推导）。7 用例过。
- **core/result_fusion.py 激活**：build_graph(enable_result_fusion=True) 加 result_fusion 节点（synthesis 前融合 sql/career/web → fusion_context）；AllowedNode/RouteDecision 补 result_fusion；开关关闭保持原路径。图双路径编译通过。
- **未完成**：Agent Bus / Reflexion 尚未接入 worker 调用点（bus 未在 graph_builder 实例化、reflexion 未挂 match/career）——下一步按蓝图 3.1/3.2 接入。

### 环境坑（记录）
- **pytest 输出被吞 + segfault(EXIT=139)**：zxf 环境 sentence_transformers/chromadb 原生库顺序冲突，pytest 收集阶段静默崩溃，连 junitxml 都不生成。验证方式：脚本直接跑断言（不 import api.dependencies）+ 小组跑。CI Ubuntu 不受影响。

## 2026-08-06 第三轮：write_Agent + 雪峰对话语料库落地

### write_Agent（蓝图单一写权限落地）
- 新建 agents/workers/write_agent.py：WriteAgent 类——写前校验（MIN_TEXT_LEN=40 噪声过滤）、去重（source hash，块号 split("#") 归一化——**教训：块级 source user:web/x#1 与基础 source user:web/x 不一致导致去重失效，已修**）、同步双写（RAG JSON user:web/ 域 + Chroma add_documents）、async 包装（to_thread）
- 接入 build_graph(enable_write_agent=True)：web_search_agent → write_agent → (fusion) → synthesis；读 state.web_search_pages 写前 5 页

### 雪峰对话语料库（Milvus + 学科 embedding）
- 新建 tools/xuefeng_store.py：XuefengStore——对话切块 _split_turns（轮次边界，MAX_TURN_CHARS=800 超长按句切）、embedding 复用统一模型（本地优先）、Milvus 后端（MILVUS_ENABLED=1 + HNSW/COSINE，pymilvus MilvusClient 探活，不可达降级 Chroma zx_xuefeng collection）、专业关键字窗口词检索（**教训：re.findall 整句当一词无法命中，改为 2-3 字滑动窗口词**，如"学医要多少钱"→"学医"/"医要"/"多少"）+ 向量混合，向量失败关键字兜底
- 新建 scripts/build_xuefeng_corpus.py：内置 3 条种子语料（医学/冲稳保/城市vs学校），data/xuefeng_dialogues/ 目录放真实对话
- KB_SCOPES 加 xuefeng 域（user:xuefeng/）与 web 域（user:web/）
- synthesis_agent 接入：build_synthesis_agent(llm, feedback_store, xuefeng_store)，生成时 to_thread(xuefeng_store.search) 检索雪峰原话注入 payload.xuefeng_style_refs 作为风格锚点（失败静默跳过）
- 测试 tests/test_write_agent_xuefeng.py（WriteAgent 往返/去重/噪声、切块、关键字窗口词、降级、synthesis 注入、无 xuefeng_store 兼容）

### 验证
- 图编译 4 组合全过（全关/write_agent/融合+write_agent/xuefeng_store）
- 语料 JSON 已生成 6 条（3 场对话 × 2 turn）；真实库 Milvus 未启动（docker daemon 未开），用 Chroma 降级
- 环境：sentence_transformers 加载触发 segfault → 用脚本断言 + mock 验证（绕开 embed）
