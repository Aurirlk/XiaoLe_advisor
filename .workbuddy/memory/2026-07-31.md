# 2026-07-31

## 企业级代码审查（全项目）

对「小乐AI 高考志愿填报助手」做了完整的商业化企业级代码审查，产出 `交接手册.md`（根目录）。

**审查基准**：LangGraph 官方实践 / 企业级 RAG（LlamaIndex·RAGAS）/ MCP / 实时语音（LiveKit·Pipecat）/ Agent Harness（OpenAI Evals·LangSmith）/ OWASP API Top 10 / 12-Factor / 个人信息保护法

**结果**：P0 阻断 14 项、P1 严重 22 项、P2 改进 15 项。所有 P0 已在本机复核验证。

### 必须记住的项目真相（避免重复踩坑）

1. **仓库内有 4 份重复代码树**，且已漂移：
   - `agents/api/` = `api/` 的 6 月陈旧快照
   - `configs/core/` = `core/` 旧副本（**最危险**，grep 定位时极易改错文件）
   - `backend/app/` = `api/` 副本，且**中文编码损坏有 SyntaxError**
   - `skills/tests/`、`tests/tools/` 同名双份
   → 唯一真源是 `api/` `core/` `tools/` `tests/`

2. **登录当前是免密的**：`api/routers/auth_router.py` 的"测试模式"允许任意手机号免密登录，输 `13800000000` 直接得 admin，无 token/无效 token 时 `/auth/me` 也返回 admin。

3. **主推荐链路必崩**：`agents/synthesis_agent.py:214,222` 使用了未定义的 `profile` 变量，被 `safe_node_call` 吞掉后伪装成「服务暂时繁忙」。

4. **密钥已入库**：`configs/.config.yaml:46` 的 MIMO_API_KEY、`configs/neo4j_config.yaml:8` 的密码均被 git 跟踪（.gitignore 只挡了 .env）。

5. **"名不副实"模块清单**（看起来做了实际是死代码/占位符）：
   `core/harness.py`（实为数据引导模块 + 重复定义致端点 500）、`core/evaluation.py`（零引用）、`core/feedback_loop.py`（假闭环）、`core/agent_bus.py`（零引用）、`core/reflexion_agent.py`（零引用）、`core/vad_detector.py`（Silero 模型不存在，恒走能量阈值）、ASR 流式引擎（类不存在，静默降级）、TTS 缓存与长文本分句（配置声明但无实现）、流式输出（伪流式）、WS 实时语音（前端未接线）、crm_tasks（结果无人消费）。

6. **ASR 在当前环境 100% 不可用**：模型目录不存在 + `funasr` 不在 requirements + 无 ffmpeg。

7. **跑测试前要 `unset DEEPSEEK_API_KEY`**：`tests/test_supervisor_routing.py:228` 检测到 key 会真调 API 烧钱。

8. **无任何 CI**（`.github/workflows/` 不存在），`requirements.txt` 零版本锁定。

### 该项目做得好的地方（勿误删）

`tools/function_tools.py` 工具层（Pydantic schema 规范）、`core/tool_retry.py` 分级 ToolResult + 指数退避、SQL/Cypher 全参数化无注入、`tools/page_fetcher.py` SSRF 防护、`supervisor_agent.py` 路由白名单 + 三级 JSON 回退解析、`rag_tools.py:132` 标准 RRF、feedback_store 人工审核数据飞轮、`synthesis_guard`/`red_team_auditor` 确定性风控（单测扎实）。

## 2026-07-31 第二轮：P0 修复工程（接续上午审查）

### 已完成修复（全部回归冒烟通过：api.main 全量 import OK）

- **P0-10/11 接口防护**：新建 `api/security_utils.py`（read_limited 流式限长/MIME 白名单/Pillow 头校验/RateLimiter 滑动窗口/require_user 生产强制-开发放行/authenticate_ws/ws_semaphore/WS_IDLE_TIMEOUT），接入 voice_router（ASR/TTS 限流+鉴权+2000 字 TTS 上限）、vision_router（20MB→10MB 流式+校验）、harness_router（upload/upload-and-analyze）、ws_router（token 鉴权+semaphore+空闲超时+aget_state）。WS 主循环必须在 `async with sem:` 内（曾因缩进错位导致 sem 失效）。
- **P0-8 Checkpointer**：重写 `core/checkpoint_manager.py`——memory/sqlite/postgres 三后端；生产禁 memory（fail-fast）；postgres 需 lifespan 内 `await aget_saver()`（main.py _check_graph 已接入）；TTL 清理 `cleanup_old_checkpoints`（CHECKPOINT_TTL_DAYS>0 时 lifespan 每日跑）；扩展包缺失抛 RuntimeError 而非降级。
- **P0-13 CRM schema 断裂**：`core/crm_manager.py` 成为 user_profiles 单一 schema 真相源（含全部认证+画像列）；`_MIGRATION_COLUMNS` 全量列清单 + PRAGMA 检查 + ALTER ADD COLUMN（注意：SQLite ADD COLUMN 不允许 `DEFAULT (datetime('now'))`，迁移用常量 ''）；`scripts/init_sqlite.py` 改为 import 复用（删重复 DDL）。**手机号 SHA-256 脱敏存储**（hash_phone + PHONE_HASH_SALT，生产必填 fail-fast），load/save/等值查询/auth_router 全走哈希，存量明文行自动迁移，JWT 仍带明文 phone 供展示。
- **P0-7 异步化**：career_agent 改 async + asyncio.gather 并行 4 源；match_agent Neo4j to_thread；synthesis_agent 补丁 YAML lru_cache（clear_feedback_patches_cache 导出）；stream_router 3 处 get_state→aget_state。
- **P0-1 git 卫生**：.gitignore 加 configs/.config.yaml，`git rm --cached` 完成。**历史里仍有旧 key，需人工撤销 + filter-repo**。

### 环境发现（重要）

- **zxf conda 环境（D:/Anaconda/envs/zxf）依赖齐全**（fastapi/langgraph 1.2.1/sqlalchemy/redis/chromadb 都有），是跑测试的正确环境。
- **已知 segfault**：`import core.emotion_analyzer` 后再 `import api.dependencies` → Segmentation Fault（sentence_transformers 原生库加载顺序冲突）。与代码改动无关（原始代码同样复现），正常启动路径不受影响。已记入手册。
- 环境缺 langgraph-checkpoint-sqlite / langgraph-checkpoint-postgres / psycopg（生产选 sqlite/postgres 后端需先装）。

### 新增环境变量（已写 .env.example）

JWT_SECRET / PHONE_HASH_SALT（生产必填）、CHECKPOINT_BACKEND / DATABASE_URL / CHECKPOINT_TTL_DAYS、API_AUTH_REQUIRED、WS_MAX_CONN / WS_IDLE_TIMEOUT_SECONDS、MAX_AUDIO/IMAGE/DOC_UPLOAD_MB、RATE_LIMIT_ASR/TTS/VISION/HARNESS_UPLOAD。

## 2026-07-31 第三轮：P1/P2 批量修复 + skills 调研 + 交付手册

### P1 修复（P0 之后）
- P1-2 decision_detector 双重 LLM：删 LLM 调用（硬边覆盖 next_node，纯浪费），只产结构化 hints
- P1-3 state reducers：state_schema 加 `_merge_dict`/`_last_not_none`/`operator.add`——仅对增量语义 key（hesitation_signals/web_search_pages/missing_profile_fields/progressive_questions/missing_info/question_history）加 add；sql_results/profile_history 是 worker 返回全量**不能**加（会重复），保持覆盖
- P1-7 RAG 切分：rag_router `_chunk_text(chunk_size=512, overlap=64)` 递归切分（边界<50%硬切），metadata 加 page/chunk_id
- P1-10 ColBERT 伪降级：multi_embedding_store 删静默 fallback→dense（会导致 RRF 里 dense 双倍权重），缺失返回空+日志；补了真 ColBERT 晚期交互实现（q@d.T max sum）
- P1-17 WS OOM：audio_buffer 5MB 上限 + ASR 单例（保留熔断器状态）
- P1-19 ASR 热词：OpenAI 兼容 provider data["prompt"]=DEFAULT_ASR_HOTWORDS（平行志愿/投档线/位次…）
- P1-21 Celery：acks_late + reject_on_worker_lost + task_track_started；broker db1/result db2 分开；beat 改 crontab（替换浮点秒）
- P1-22 pytest markers：`@pytest.mark.llm` + pytest.ini `-m "not llm"`；test_admin_routes 适配 X-Admin-Key 鉴权；test_anti_hallucination 拆"业务异常兜底/代码缺陷暴露"两测；crawl_gaokao.py 修 2 处混入的 JS 语法（.filter/.match → Python）+ 补 import re

### P2 修复
- P2-1/2/3 删死代码：core/agent_bus.py、reflexion_agent.py、evaluation.py
- P2-5 AllowedNode 补 web_search_agent
- P2-8 routing_tuner：lru_cache + 原子写（mkstemp+os.replace）+ clear_tuning_cache
- P2-11 _web_search 不再裸吞异常（返回"服务不可用"文案区分无结果）
- P2-12 情感 TTS 支持 mimo（style 参数）

### 环境结论（重要，勿再死磕）
**zxf 环境 pytest 全量跑必 segfault**（sentence_transformers/chromadb 原生库顺序+内存），单文件/小组（≤5 文件）稳定、`-X faulthandler` 下稳定、CI 干净 Ubuntu 不受影响。验证方式：分片跑。**已记入手册且 conftest 提前 import 会让单文件也崩（别加那种 conftest）**。

### skills/prompt 调研
- skills：14 活跃 + 删 6 死代码（reality_mapper/quantitative_scorer/hard_filter/family_mediator/comparison_engine/application_form），README/docs 目录树已同步
- prompts：configs/prompts/zx_system_prompt.md（张雪峰风格+反幻觉，synthesis 主 prompt）+ synthesis_system_prompt.txt（fallback）；各 agent 内嵌 prompt

### 交付
- 新建 `交付手册.md`（架构/部署/配置/功能/测试/安全/已知边界/代码地图/prompt 工作流），**删除原 交接手册.md**
- backend 迁移：api/crud_services.py + api/response.py + 5 个 router（admin_crud 加 X-Admin-Key 鉴权；zhihu ACCESS_SECRET→ZHIHU_ACCESS_SECRET 环境变量；user_notification 修 get_current_user 未 await bug）

## 2026-07-31 第四轮：5 大架构项 + P1-P7 真缺陷修复（v0.3.0）

### 完成 5 项
1. **真流式**：stream_router/ws_router 改 `stream_mode=["updates","messages"]` 双流；synthesis_agent `llm.ainvoke`→`llm.astream` 聚合（ainvoke 走 _agenerate 不触发 token 回调！）；dependencies.py LLM 加 `streaming=True`；只放行 `AIMessageChunk` 增量（过滤 supervisor 推理 + 完整 AIMessage 防重复拼接）；无流式兜底整段
2. **Embedding 统一**：新建 `tools/embedding_config.py` 单一真源（vector_config.yaml 读取，支持 ${ENV_VAR}）；vector_store 与 multi_embedding_store 共用；`_resolve_local_model_path` 本地优先防联网；`scripts/rebuild_embedding_index.py` 重灌（--force）
3. **RAG 评估体系**：`scripts/rag_eval/`（30 条领域标注 eval_dataset.json + run_rag_eval.py 算 Recall@k/Precision@k/MRR/NDCG@k + 可选 LLM faithfulness）
4. **权重配置化**：rag_config.yaml `fusion` 段（rrf_k/各路 weight/recall_multiplier/keyword_min_recall），rag_tools 与 multi_embedding_store 全读配置；**评估标定 dense_weight 1.0→0.3**（MiniLM 噪声大，满权重 Recall@3 0.556→0.356，调低后 0.567）
5. **intent_tracker 接线**（断链修复）：`_make_crm_callback` 补 log_intent + log_decision_journey（从 state 取 scene/path/decision/signals/query/response）；graph_builder._synthesis_node 把 synthesis 结果合并进 state 再回调（否则回调读不到最终回复）；init_sqlite 建 user_intent_log/user_decision_journey 表（DDL 单一真源在 intent_tracker.py）；phone 空→anonymous 兜底

### P1-P7 真缺陷（评估体系暴露，比预想严重）
- **FTS5 路是死的**：rag_fts 从未灌数据（0 行）+ unicode61 中文无效 → trigram tokenizer + init_sqlite 幂等灌 2287 条
- **Chroma 只有 10/2287 条**：collection_has_data "非空即跳过" 永不同步 → 改按条数比对重灌
- **中文分词失效**：_tokenize 用 re.split(r"\W+") 中文整句当单 token → CJK 字符级+英文单词级
- **chromadb 1.5.9 Windows 双坑**（勿再踩）：① persist_dir 绝对路径（D:\...）开 hnsw 报 "Error loading hnsw index"，项目根相对路径正常 → _normalize_persist_dir 统一相对路径；② 非默认 Settings(anonymized_telemetry=False) 触发 SharedSystemClient identifier 冲突 → 用默认 settings；③ 另加 hnsw:sync_threshold=100 collection 元数据（默认 1000 时 2287 条尾部 239 条永不落盘）

### 指标对比（30 条评估集）
修复前 Recall@3=0.189 MRR=0.391 → 修复后 Recall@3=0.567 MRR=0.666

### 测试
新增 tests/test_streaming_mode.py（4 用例）、test_intent_tracker_chain.py（7 用例）、test_vector_store_persist.py（5 用例）；修正 test_hybrid_search 的 RRF"分数相同"错误断言（rank1>rank2 才符合算法）。本轮全部 27 passed。
